# Pinned to the versions used for every number reported in the paper.
# Python 3.13.6 (CPython). Single-threaded; no GPU, no MPI.
numpy==2.3.5
scipy==1.17.0
sympy==1.14.0
mpmath==1.3.0
matplotlib==3.10.8
